spring.application.name=portfolio-be
management.endpoints.web.exposure.include=*
management.endpoint.mappings.enabled=true

logging:
  level:
    org.springframework: INFO
    org.hibernate.SQL: INFO
